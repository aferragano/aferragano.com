look at all of these devs
<br>
<% @devs.each do |d|%>
	<%= d.title %><br>
	<%= d.image_path %><br>
	<%= d.user.username %><br>
	<%= link_to "View More", ( dev_path(d))%>
	<%= link_to "Edit", ( edit_dev_path(d))%>

<%end%>